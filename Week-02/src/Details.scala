

object Details {
  def main(args: Array[String]): Unit = {
    var initial: Char = 'F'
    var day: Int = 12
    var month: String = "Jan"
    var year: Int = 2001
    
    println("My first name initial is " + "\"" + initial + "\"" 
             + " and I was born on " + day + "/" + month + "/" + year)
    }
}